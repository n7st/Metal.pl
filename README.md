# Metal - IRC Framework

## About

Metal is an IRC bot framework written in Perl using Moose and
POE::Component::IRC.

## Installation

* `./bin/metal -mode test`
* `./bin/metal -mode bootstrap`

## Running the bot

* `./bin/metal -run`

